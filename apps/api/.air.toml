root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/api ./cmd/api"
  bin = "./tmp/api"
  include_ext = ["go", "yaml", "yml"]
  exclude_dir = ["bin", "tmp"]
  exclude_regex = ["_test\\.go$"]
  delay = 200
  stop_on_error = true

[log]
  main_only = true
